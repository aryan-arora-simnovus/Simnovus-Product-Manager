services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/keys:/app/keys:ro

  frontend:
    build: ./frontend
    ports:
      - "8090:8090"
    environment:
      - VITE_API_URL=http://localhost:8080
    depends_on:
      - backend
